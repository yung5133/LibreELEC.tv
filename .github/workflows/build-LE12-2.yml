name: build-LE12-2
on:
  workflow_dispatch:
    inputs:
      le_periodic_version:
        description: "Version and Perodic builds"
        default: nightly
        required: true
        type: choice
        options:
          - nightly
          - official
      gitref_to_build:
        description: "gitref tag or hash to build"
        default: libreelec-12.2
        required: true
        type: string
      target:
        description: "targets to build"
        default: RK3399.aarch64
        required: true
        type: choice
        options:
          - all
          - RK3399.aarch64

env:
  clean_le: no_clean_le
  debug: debug
  ephemeral: ephemeral
  upload: upload

jobs:
  check_date:
    runs-on: ubuntu-latest
    name: Checkout ~/LibreELEC.tv and check latest commit
    outputs:
      should_run: ${{ steps.should_run.outputs.should_run }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: libreelec-12.2
          fetch-depth: 2
          repository: "${{ github.repository_owner }}/LibreELEC.tv"
          path: "LibreELEC.tv"
      - name: print latest_commit of checked out ~/LibreELEC.tv
        run: |
          cd LibreELEC.tv
          git rev-parse HEAD

      - name: Restore Cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            LibreELEC.tv/sources
          key: sources-Rockchip-RK3399-aarch64-leez-p710
          restore-keys: |
            sources-Rockchip-RK3399-aarch64-leez
            sources-Rockchip-RK3399-aarch64
            sources-Rockchip-RK3399
            sources-
  
      - name: download tool to cache source path
        run: |
          cd LibreELEC.tv
          PROJECT=Rockchip DEVICE=RK3399 ARCH=aarch64 UBOOT_SYSTEM=leez-p710 tools/download-tool
        continue-on-error: true

      - name: Save Cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: |
            LibreELEC.tv/sources
          key: sources-Rockchip-RK3399-aarch64-leez-p710
    
      - id: should_run
        continue-on-error: true
        name: check latest commit is less than a day
        if: ${{ github.event_name == 'schedule' }}
        run: |
          cd LibreELEC.tv
          test -z "$(git rev-list --after='24 hours' $(git rev-parse HEAD))" && echo "should_run=false" >> $GITHUB_OUTPUT
          exit 0

  # Rockchip
  RK3399_aarch64-12_2:
    name: "RK3399.aarch64 12.2"
    # Only run if there has been a commit in the last 24 hours
    needs: check_date
    if: |
      ( needs.check_date.outputs.should_run != 'false' )
        && ( github.event.inputs.target == 'all' || github.event.inputs.target == 'RK3399.aarch64' || github.event_name == 'schedule' )
    uses: ./.github/workflows/yml-uses-make-image-LE12-2.yml
    with:
      clean_le: no_clean_le
      debug: debug
      ephemeral: ephemeral
      upload: upload
      buildcmd: "make image"
      gitref: ${{ github.event.inputs.gitref_to_build || 'libreelec-12.2' }}
      group: RK3399_aarch64-12_2
      project: Rockchip
      arch: aarch64
      device: RK3399
      uboot: leez-p710
      targetbuilddir: build.LibreELEC-RK3399.aarch64-12.2-devel
      buildperiodic: ${{ github.event.inputs.le_periodic_version || 'nightly' }}
      ccachecachesize: 10G
      version: "12.2"
      file_extension: img.gz,tar
    secrets: inherit
